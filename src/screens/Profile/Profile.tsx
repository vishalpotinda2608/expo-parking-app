import { View, Text } from "react-native";
import React from "react";
import { Button } from "react-native";

const ProfileScreen = () => {
  return (
    <View>
      <Text>Profile</Text>
      <Button title="SHOW PROFILE" />
    </View>
  );
};

export default ProfileScreen;
